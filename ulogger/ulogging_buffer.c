#include "ulogging_buffer.h"

typedef uLoggingBuffer uBuffer;


int ulogging_buffers_init(ulogging_buffer, start, buffer_capacity) {
    for
}

void push_event_to_buffer(EventType event_type, timestamp time, ...) {

}